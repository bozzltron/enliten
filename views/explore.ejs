<div id="enliten-viewer" ng-app="enliten-viewer">
    <div  ng-controller="ViewerController as vc">

        <!-- HERO -->
        <section ng-if="!step" class="module module-parallax bg-light-30" data-background="assets/images/section-7.jpg">

            <!-- HERO TEXT -->
            <div class="container">

                <div class="row">
                    <div class="col-sm-12 text-center">
                        <h1 class="mh-line-size-3 font-alt m-b-20"><%= path.name %></h1>
                    </div>
                </div>

            </div>
            <!-- /HERO TEXT -->

        </section>
        <!-- /HERO -->

        <!-- VORTEX BUTTONS -->
        <section  ng-if="!step" class="module">

            <div class="container">

                <!-- MODULE TITLE -->
                <div class="row">
                    <div class="col-sm-6 col-sm-offset-3">
                        <h2 class="module-title font-alt"><%= path.description %></h2>
                    </div>
                </div>
                <!-- /MODULE TITLE -->

                <div class="row">
                    <div class="col-sm-6 col-sm-offset-3">
                        <p class="text-center"><button data-step="0" ng-click="vc.next()" type="submit" class="btn-block btn btn-g btn-round btn-lg"><i class="fa fa-play"></i> Start</button>
                    </div>
                </div>

                <div  ng-if="resume == true" class="row">
                    <div class="col-sm-6 col-sm-offset-3">
                        <p class="text-center"><button ng-click="vc.resume()" class="btn-block btn btn-g btn-round btn-lg"><i class="fa fa-fast-forward"></i> Pick Up Where You Left Off</button></p>
                    </div>
                </div>

            </div>

        </section>
        <!-- /VORTEX BUTTONS -->

        <section ng-if="step" class="">

            <div class="container">

                <div class="row step fadeInRight animated">
                    <div class="col-sm-12">

                        <a  target="_blank" ng-if="step.type == 'Url'" ng-href="{{step.url}}">
                            <img ng-if="step.type == 'Url'" style="margin:auto" class="margin-auto img-responsive" ng-src="{{step.iurl}}"/>
                        </a>

                        <div ng-if="step.type == 'Embed code'" ng-bind-html="step.url" class="embed-responsive embed-responsive-16by9" >

                        </div>
                        <a class="text-center" ng-if="step.type == 'Photo'" ng-href="{{step.url}}" target="_blank"><img ng-if="step.type == 'Photo'" class="margin-auto img-responsive" ng-src="{{step.iurl}}" /></a>

                        <h2 class="animated">{{step.name}}</h2>
                        <p class="lead">{{step.description}}</p>

                        <div>
                            <button ng-click="vc.back()" class="pull-left btn btn-g btn-round btn-lg"><i class="fa fa-step-backward"></i> Go Back</button>
                            <button ng-click="vc.next()" class="pull-right btn btn-g btn-round btn-lg"><i class="fa fa-step-forward"></i> Continue</button>
                        </div>
                    </div>
                </div>

            </div>

        </section>

    </div>
</div>

<!-- Javascript files -->
<script>
    var path = '<%= path.id %>';
</script>
<script src="/js/ux/jquery-2.1.3.min.js"></script>
<script src="/js/ux/jquery.touchSwipe.min.js"></script>
<script src="/js/dist/viewer-built.js"></script>
